% Handout build: flattens Beamer overlays into static slides
% for distribution. Used by .github/workflows/build.yml so the
% published PDF is print-ready and one PDF page == one slide.
\PassOptionsToClass{handout}{beamer}
\input{main.tex}
