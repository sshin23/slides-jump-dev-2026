\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usepackage{mathtools}
\usepackage{multirow}
\usepackage{booktabs}
\usepackage{listings}

\lstdefinelanguage{julia}{
  morekeywords={using,end,function,return,for,in,if,else,elseif,while,let,do,begin,struct,abstract,mutable,type,const,global,local,where,break,continue,quote,true,false,nothing,Inf},
  morekeywords=[2]{ExaCore,ExaModel,Model,Optimizer,CUDABackend,ROCBackend,OneAPIBackend,MetalBackend,OpenCLBackend,CPU,CuArray},
  morekeywords=[3]{@add_var,@add_par,@add_obj,@add_con,@add_con!,@add_expr,@register_univariate,@register_bivariate,@variable,@constraint,@objective,@NLconstraint,@NLobjective},
  sensitive=true,
  morecomment=[l]{\#},
  morestring=[b]",
}
\lstset{
  language=julia,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{mitred}\bfseries,
  keywordstyle=[2]\color{mitblue},
  keywordstyle=[3]\color{mitorange}\bfseries,
  commentstyle=\color{mitdarkgray}\itshape,
  stringstyle=\color{mitdarkgray},
  showstringspaces=false,
  columns=fullflexible,
  keepspaces=true,
  breaklines=false,
  upquote=true,
  backgroundcolor=\color{mitlightgray!30},
  frame=single,
  framerule=0pt,
  framesep=4pt,
  xleftmargin=4pt,
  xrightmargin=4pt,
  aboveskip=4pt,
  belowskip=4pt,
}
\usepackage[style=authoryear-comp,url=false,date=year,doi=false,isbn=false,eprint=true,maxnames=1,minnames=1,backend=biber,natbib=true]{biblatex}

% ------------------------------------------------------------
% MIT COLOR PALETTE  (from MIT Brand Guidelines)
% ------------------------------------------------------------
\definecolor{mitred}{HTML}{A31F34}       % Primary MIT red
\definecolor{mitdarkgray}{HTML}{545454}  % Neutral dark gray
\definecolor{mitlightgray}{HTML}{C7C7C7} % Light gray
\definecolor{mitorange}{HTML}{D56F00}    % Secondary accent
\definecolor{mitblue}{HTML}{8A8BFF}      % Accent cool tone

% ------------------------------------------------------------
% APPLY TO TITLES
% ------------------------------------------------------------
\setbeamercolor{title}{fg=mitred}
\setbeamercolor{subtitle}{fg=mitdarkgray}
\setbeamercolor{frametitle}{fg=mitred}
\setbeamercolor{framesubtitle}{fg=mitdarkgray}
\setbeamercolor{section title}{fg=mitred}
\setbeamercolor{subsection title}{fg=mitdarkgray}

\setbeamercolor{structure}{fg=mitred}
\setbeamercolor{itemize item}{fg=mitred}
\setbeamercolor{itemize subitem}{fg=mitdarkgray}
\setbeamercolor{enumerate item}{fg=mitred}
\setbeamercolor{enumerate subitem}{fg=mitdarkgray}

\setbeamercolor{caption name}{fg=mitred}
\setbeamercolor{caption}{fg=mitdarkgray}

\setbeamertemplate{blocks}[rounded][shadow=false]
\setbeamercolor{block title}{fg=mitred,bg=mitorange!20}
\setbeamercolor{block body}{fg=mitdarkgray,bg=mitorange!20}
\setbeamercolor{block title alerted}{fg=mitred,bg=mitred!20}
\setbeamercolor{block body alerted}{fg=mitdarkgray,bg=mitred!20}
\setbeamercolor{block title example}{fg=mitred,bg=mitdarkgray!20}
\setbeamercolor{block body example}{fg=mitdarkgray,bg=mitdarkgray!20}

\setbeamercolor{alerted text}{fg=mitred}
\setbeamercolor{example text}{fg=mitblue}

\setbeamercolor{palette primary}{fg=white,bg=mitred}
\setbeamercolor{palette secondary}{fg=white,bg=mitdarkgray}
\setbeamercolor{palette tertiary}{fg=white,bg=mitdarkgray}
\setbeamercolor{palette quaternary}{fg=white,bg=mitdarkgray}

\setbeamercolor{separation line}{bg=mitlightgray}
\setbeamercolor{fine separation line}{bg=mitlightgray}

\hypersetup{
  colorlinks=true,
  linkcolor=mitred,
  urlcolor=mitred,
  citecolor=mitdarkgray
}

\setbeamersize{text margin left=0.5cm, text margin right=0.5cm}

\setbeamertemplate{navigation symbols}{}

\setbeamertemplate{footline}{%
  \leavevmode%
  \usebeamerfont{footline}
  \usebeamercolor[fg]{footline}
  \tiny
  \tikz[remember picture, overlay,xscale=0.875,transform shape]{
    \node[anchor=south] at (current page.south) {\insertshorttitle};
    \node[anchor=south west] at (current page.south west) {Sungho Shin--- \href{mailto:sushin@mit.edu}{\tt sushin@mit.edu}};
    \node[anchor=south east] at (current page.south east) {\insertframenumber/\inserttotalframenumber};
  }
}

\setbeamercolor{footline}{fg=mitdarkgray}
\setbeamerfont{footline}{size=\scriptsize}

\addbibresource{shin.bib}
\AtEveryCitekey{\clearfield{pages}}
\AtEveryCitekey{\clearfield{volume}}
\AtEveryCitekey{\clearfield{number}}
\AtEveryCitekey{\clearfield{primaryclass}}
\AtEveryCitekey{\clearfield{edition}}
\AtEveryCitekey{\clearfield{publisher}}
\AtEveryCitekey{\clearfield{series}}
\AtEveryCitekey{\clearfield{address}}
\DeclareNameAlias{default}{labelname}
\renewbibmacro{in:}{}

\newcommand{\refbr}[2][]{%
  \begin{tikzpicture}[remember picture, overlay,xscale=0.875,transform shape,inner sep=2pt]
    \node[anchor=south west,align=left,font=\tiny,color=mitdarkgray,text width=1.2\linewidth,yshift=10pt] at (current page.south west) {#1 #2};
  \end{tikzpicture}%
}

\newcommand{\joint}[2][]{%
  \begin{tikzpicture}[remember picture, overlay, xscale=0.875,transform shape,inner sep=2pt]
    \node[anchor=south west,align=left,font=\tiny,color=mitdarkgray] at (current page.south west) {#1 #2};
    \node[anchor=south east,align=left,font=\tiny,color=mitdarkgray] at (current page.south east) {\insertframenumber/\inserttotalframenumber};
  \end{tikzpicture}%
}

\graphicspath{{figs/}}

\title[Can ExaModels Power JuMP on GPUs?]{Can ExaModels Power JuMP on GPUs?}
\subtitle{Pattern-aware modeling and GPU-native automatic differentiation}
\author{
  Sungho Shin\\
  \url{shin.mit.edu}
}
\institute{
  Massachusetts Institute of Technology
}

\date{JuMP-dev 2026 \quad\textbar\quad Edinburgh}

\begin{document}

\frame{
  \joint{
    Joint work with:
    Fran\c{c}ois Pacaud (MINES Paris-PSL),
    Alexis Montoison (AMD),
    Michel Schanen (Argonne),
    Mihai Anitescu (Argonne)
  }
  \thispagestyle{empty}
  \titlepage
}

\setbeamertemplate{footline}[content]
\begin{frame}{Who are we?}
  \begin{center}
    \url{https://github.com/exanauts/ExaModels.jl}\\[.15in]
    \scriptsize
    \begin{tabular}{ccccc}
      \includegraphics[width=.6in]{shin.jpeg} &
      \includegraphics[width=.6in]{michel2323.jpeg} &
      \includegraphics[width=.6in]{amontoison.jpeg} &
      \includegraphics[width=.6in]{blegat.png} &
      \includegraphics[width=.6in]{klamike.png} \\
      Sungho Shin & Michel Schanen & Alexis Montoison & Beno\^it Legat & Michael Klamkin \\
      {\color{mitdarkgray}\texttt{@sshin23}} &
      {\color{mitdarkgray}\texttt{@michel2323}} &
      {\color{mitdarkgray}\texttt{@amontoison}} &
      {\color{mitdarkgray}\texttt{@blegat}} &
      {\color{mitdarkgray}\texttt{@klamike}} \\[1.5ex]
      \includegraphics[width=.6in]{frapac.jpeg} &
      \includegraphics[width=.6in]{andrewrosemberg.jpeg} &
      \includegraphics[width=.6in]{FHoltorf.png} &
      \includegraphics[width=.6in]{jbcaillau.jpeg} &
      \includegraphics[width=.6in]{hfytr.png} \\
      Fran\c{c}ois Pacaud & Andrew Rosemberg & Flemming Holtorf & Jean-Baptiste Caillau & Archim J \\
      {\color{mitdarkgray}\texttt{@frapac}} &
      {\color{mitdarkgray}\texttt{@andrewrosemberg}} &
      {\color{mitdarkgray}\texttt{@FHoltorf}} &
      {\color{mitdarkgray}\texttt{@jbcaillau}} &
      {\color{mitdarkgray}\texttt{@hfytr}}
    \end{tabular}
  \end{center}
\end{frame}

% =============================================================
\section{Why GPU-native modeling, only for NLPs?}
% =============================================================
\begin{frame}{Why a GPU-compatible modeling language?}{LP / QP / conic / DCP --- not strictly needed}
  \begin{columns}[c]
    \begin{column}{.62\textwidth}
      \begin{itemize}
      \item Canonical forms ($A, b, c$, cone description) $\Rightarrow$ \alert{one-shot CPU$\to$GPU copy} of problem data
      \item Solver \alert{can iterate entirely on the device}
      \item Solution copied \alert{once back} to CPU
      \item These transfers \alert{can be costly}, but the cost is \alert{amortized} over many solver iterations
      \end{itemize}
    \end{column}
    \begin{column}{.36\textwidth}
      \centering
      \begin{tikzpicture}[
        every node/.style={font=\scriptsize, align=center},
        host/.style={draw=mitdarkgray, rounded corners=2pt, fill=mitlightgray!40, minimum width=2.6cm, minimum height=0.9cm, inner sep=2pt},
        dev/.style={draw=mitred, rounded corners=2pt, fill=mitred!15, minimum width=2.6cm, minimum height=0.9cm, inner sep=2pt},
      ]
        \node[host] (cpu) at (0, 2.0) {\textbf{CPU}\\host memory};
        \node[dev]  (gpu) at (0, 0)   {\textbf{GPU}\\device memory};
        \draw[->, very thick, mitred] ([xshift=-5mm]cpu.south) -- ([xshift=-5mm]gpu.north)
          node[midway, left=1pt, font=\tiny, color=mitdarkgray, align=right]
          {problem\\(once)};
        \draw[->, very thick, mitred] ([xshift=5mm]gpu.north) -- ([xshift=5mm]cpu.south)
          node[midway, right=1pt, font=\tiny, color=mitdarkgray, align=left]
          {solution\\(once)};
      \end{tikzpicture}
    \end{column}
  \end{columns}
  \vfill
  {\tiny\itshape\color{mitdarkgray} Cartoon depicts only the large problem-data transfers (e.g., matrix factors). GPU kernels themselves are launched from CPU regardless.}
\end{frame}

\begin{frame}{Why a GPU-compatible modeling language?}{NLP --- the picture is different}
  \begin{columns}[c]
    \begin{column}{.62\textwidth}
      \begin{itemize}\itemsep2pt
      \item Solver \alert{queries the modeling layer at every iteration} for
        $f(x),\; g(x),\; \nabla f(x),\; \nabla g(x),\; \nabla^2_{xx} L(x,\lambda)$
      \item Each query depends on the current iterate---\alert{no one-shot transfer}
      \item Without GPU-native callbacks, every iteration pays a CPU$\leftrightarrow$GPU round-trip: \alert{per-iteration cost, not amortized}
      \item<2-> JuMP today does \alert{not} emit GPU-native callbacks. Fine: modeling infrastructure stays on CPU, \alert{as long as the callbacks themselves run on the device}
      \end{itemize}
    \end{column}
    \begin{column}{.36\textwidth}
      \centering
      \begin{tikzpicture}[
        every node/.style={font=\scriptsize, align=center},
        host/.style={draw=mitdarkgray, rounded corners=2pt, fill=mitlightgray!40, minimum width=2.6cm, minimum height=0.9cm, inner sep=2pt},
        dev/.style={draw=mitred, rounded corners=2pt, fill=mitred!15, minimum width=2.6cm, minimum height=0.9cm, inner sep=2pt},
      ]
        \node[host] (cpu) at (0, 2.0) {\textbf{CPU}\\host memory};
        \node[dev]  (gpu) at (0, 0)   {\textbf{GPU}\\device memory};
        \draw[<->, very thick, mitred] (cpu.south) -- (gpu.north)
          node[midway, right=2pt, font=\tiny, color=mitdarkgray, align=left]
          {each iter:\\$f, g,$\\$\nabla f, \nabla g,$\\$\nabla^2 L$};
      \end{tikzpicture}
    \end{column}
  \end{columns}
  \vfill
  {\tiny\itshape\color{mitdarkgray} Cartoon depicts only the large problem-data transfers. GPU kernels themselves are launched from CPU regardless.}
\end{frame}

% =============================================================
\section{The translation layer}
% =============================================================
\begin{frame}[fragile]{Two paths from JuMP to ExaModels}
  Load \texttt{ExaModels} + \texttt{JuMP} together---the \texttt{ExaModelsJuMP} / \texttt{ExaModelsMOI} package extensions activate automatically.\par
  \medskip
  \begin{onlyenv}<1->
  \alert{\bfseries Path 1 --- Direct conversion to an \texttt{ExaModel}}\par
\begin{lstlisting}
using ExaModels, JuMP, CUDA, MadNLPGPU

em = ExaModel(jm; backend = CUDABackend())   # JuMP -> GPU-ready ExaModel
result = madnlp(em)                          # solve via NLPModels API
\end{lstlisting}
  \end{onlyenv}
  \medskip
  \begin{onlyenv}<2->
  \alert{\bfseries Path 2 --- ExaModels as a JuMP optimizer}\par
\begin{lstlisting}
using ExaModels, JuMP, CUDA, MadNLP, MadNLPGPU

set_optimizer(jm, () -> ExaModels.Optimizer(MadNLP.madnlp, CUDABackend()))
optimize!(jm)                                # stay in JuMP, solve on GPU
\end{lstlisting}
  \end{onlyenv}
  \medskip
  \begin{itemize}
  \item<3-> Both routes share the same MOI bridge: bucket matching expression heads into templatized sums
  \item<4-> Caveat: only \texttt{@constraint} / \texttt{@objective} are supported. \alert{\texttt{@NLconstraint} / \texttt{@NLobjective} are not}
  \end{itemize}
\end{frame}

% =============================================================
\section{SIMD abstraction}
% =============================================================
\begin{frame}{An alternative to vectorization}
  \begin{itemize}
  \item Two strategies preserve indexed-constraint structure at the modeling layer:
  \end{itemize}
  \begin{columns}[t]
    \begin{column}{.48\textwidth}
      \alert{\bfseries Vectorization}
      \begin{itemize}
      \item Express each indexed family as a single matrix-algebra operation
      \item Canonical: CasADi MX, NumPy-style arrays
      \item Heterogeneous indexed structure (e.g., ACOPF power balance) requires \alert{pre-built sparse incidence matrices}
      \end{itemize}
    \end{column}
    \begin{column}{.48\textwidth}
      \visible<2->{
      \alert{\bfseries Templatizing}
      \begin{itemize}
      \item Parameterize a single expression over a \alert{data iterator}; evaluate across instances
      \item Canonical: Gravity, ExaModels
      \item Heterogeneous structure handled \alert{directly by the iterator}
      \end{itemize}
      }
    \end{column}
  \end{columns}
  \medskip
  \visible<3->{
    \begin{block}{ExaModels' SIMD abstraction}
      Each objective term / constraint = a \alert{(pattern, data iterator)} pair. One compiled derivative kernel per pattern, executed across all data points in SIMD fashion.
    \end{block}
  }
\end{frame}

\begin{frame}[fragile]{Goddard rocket in ExaModels}
\begin{lstlisting}
using ExaModels, NLPModelsIpopt

core = ExaCore(minimize = false)                      # maximize final altitude
@add_var(core, h, 0:nh; start = 1.0, lvar = 1.0)
@add_var(core, v, 0:nh; start = (i/nh*(1-i/nh) for i=0:nh))
@add_var(core, m, 0:nh; start = ..., lvar = m_f, uvar = m_0)
@add_var(core, T, 0:nh; start = T_max/2, lvar = 0.0, uvar = T_max)
@add_var(core, step, 1; start = 1/nh, lvar = 0.0)

@add_obj(core, h[nh])                                 # objective: final altitude
@add_con(core, c1, -h[i]+h[i-1] + 0.5*step[1]*(v[i]+v[i-1]) for i=1:nh)
@add_con(core, c2, ...)                               # velocity dynamics
@add_con(core, c3, ...)                               # mass dynamics
@add_con(core, h[0] - h_0); @add_con(core, v[0] - v_0); ...   # boundary conditions

result = ipopt(ExaModel(core))                        # CPU, via NLPModels
\end{lstlisting}
  \begin{itemize}
  \item<2-> Three dynamics + four boundaries = \alert{5 patterns}, regardless of \texttt{nh}
  \item<3-> Same code on GPU: change \alert{one line} ---
        \texttt{core = ExaCore(minimize = false, backend = CUDABackend())}
  \end{itemize}
\end{frame}

% =============================================================
\section{Stay in JuMP: GenOpt.jl}
% =============================================================
\begin{frame}[fragile]{GenOpt.jl \citep{legatGenOptJl2025} --- preserve patterns at JuMP-side construction}
  \texttt{GenOpt.jl} (Benoît Legat) packs ``groups of similar constraints / sums of similar terms'' into \alert{lightweight generators} that ExaModels can recognize.
\begin{lstlisting}
using GenOpt, JuMP, CUDA, MadNLP

@constraint(model, [i in 1:n], x[1,i] == x0[i],
    container = ParametrizedArray)                            # grouped constraint

@objective(model, Min,
    lazy_sum(0.5 * R[j] * u[i,j]^2 for i in 1:N, j in 1:p))   # grouped sum

set_optimizer(model, () -> GenOpt.ExaOptimizer(madnlp, CUDABackend()))
optimize!(model)
\end{lstlisting}
  \begin{itemize}
  \item<2-> Patterns are preserved at \alert{JuMP-side construction}---ExaModels receives them \alert{intact} (no scalar enumeration)
  \item<3-> Minimal model rewrite: add the \texttt{container} keyword / wrap sums in \texttt{lazy\_sum}
  \item<4-> Originally presented at JuMP-dev 2025
  \end{itemize}
  \refbr{
    \fullcite{legatGenOptJl2025}
  }
\end{frame}

% =============================================================
\section{Under the hood}
% =============================================================
\begin{frame}{Type-stable expression tree + coloring-free sparse AD}
  \alert{\bfseries Parameterized expression tree}
  \begin{itemize}
  \item Each pattern compiles to a \alert{type-stable} graph; Julia compiler specializes a dedicated evaluator per pattern
  \item<2-> Same tree, different data and arguments, \alert{no recompilation}
  \end{itemize}
  \medskip
  \visible<3->{
  \alert{\bfseries Coloring-free sparse AD}
  \begin{itemize}
  \item Standard sparse reverse-mode AD uses \alert{graph coloring}---GPU-unfriendly (irregular control flow, dynamic dispatch)
  \item<4-> ExaModels: \alert{a priori} sparsity analysis per pattern $\Rightarrow$ sparse Jacobian + Lagrangian Hessian in \alert{COO directly, no coloring round}
  \end{itemize}
  }
\end{frame}

% =============================================================
\section{GPU execution}
% =============================================================
\begin{frame}{One model, six backends}
  \begin{itemize}
  \item \alert{SIMDFunction} is the compiled artifact for one pattern: a per-data-point kernel + sparsity map
  \item<2-> CPU evaluation: \texttt{@simd} loop over data points
  \item<3-> GPU evaluation: launched via \alert{KernelAbstractions.jl} \citep{churavyKernelAbstractionsjl2025}
  \item<4-> Same model code targets:
    \begin{itemize}
    \item \texttt{CPU()} (multi-threaded) \quad $\bullet$\quad \texttt{CUDABackend()} (NVIDIA) \quad $\bullet$\quad \texttt{ROCBackend()} (AMD)
    \item \texttt{OneAPIBackend()} (Intel) \quad $\bullet$\quad \texttt{MetalBackend()} (Apple) \quad $\bullet$\quad \texttt{OpenCLBackend()} (generic)
    \end{itemize}
  \item<5-> Implementation choices that matter on GPU:
    \begin{itemize}
    \item COO (not CSC) for sparse output --- coalesced writes
    \item Objective: per-pattern buffer + reduction
    \item Gradient: sparse accumulation + compress
    \end{itemize}
  \item<6-> Currently the GPU IPM consumer is \alert{MadNLP + MadNLPGPU} \citep{shinMadNLPjl2025,pacaudCondensedspaceMethodsNonlinear2024}
  \end{itemize}
  \refbr{
    \fullcite{churavyKernelAbstractionsjl2025}\\
    \fullcite{shinMadNLPjl2025}
  }
\end{frame}

% =============================================================
\section{Where ExaModels--JuMP stands today}
% =============================================================
\begin{frame}{Status quo, May 2026}
  \small
  \alert{\bfseries Working today}
  \begin{itemize}\itemsep1pt
  \item JuMP $\to$ ExaModels via \texttt{ExaModel(jm; backend = ...)} or \texttt{ExaModels.Optimizer}
  \item Pattern-preserving JuMP authoring via GenOpt.jl \citep{legatGenOptJl2025}
  \item MadNLP + MadNLPGPU is the GPU IPM consumer \citep{shinMadNLPjl2025,pacaudCondensedspaceMethodsNonlinear2024}
  \end{itemize}
  \smallskip
  \alert{\bfseries Recently simplified}
  \begin{itemize}\itemsep1pt
  \item<2-> ExaModels.jl \href{https://github.com/exanauts/ExaModels.jl/pull/282}{\#282} (M.~Schanen, 2026-05-28): drop \texttt{ExaModelsMadNLP} / \texttt{ExaModelsIpopt} extensions $\Rightarrow$ \alert{NLPModels API directly}. Breaking: \texttt{MadNLPOptimizer} removed
  \end{itemize}
  \smallskip
  \alert{\bfseries Open}
  \begin{itemize}\itemsep1pt
  \item<3-> \texttt{@NLconstraint} / \texttt{@NLobjective} not supported by the bridge
  \item<4-> Heterogeneous indexed structure does not round-trip $\Rightarrow$ use GenOpt.jl or native
  \item<5-> OpenCL / oneAPI backends held by a GPUCompiler pin (upstream SPIR-V issue)
  \end{itemize}
\end{frame}

% =============================================================
\section{Numerical results}
% =============================================================
\begin{frame}{Benchmarks}{Luk\v{s}an--Vlc\v{e}k, COPS, PGLIB-OPF}
  \begin{itemize}
  \item ExaModels-native suites: \texttt{LuksanVlcekBenchmark.jl}, \texttt{COPSBenchmark.jl}, \texttt{ExaModelsPower.jl}
  \item<2-> \alert{CPU}: ExaModels vs JuMP vs AMPL --- function + derivative evaluation timing (AD layer only)
  \item<3-> \alert{GPU}: same model code on CUDA, ROCm, oneAPI, Metal, OpenCL; CPU-8T baseline
  \item<4-> Up to \alert{two orders of magnitude} acceleration over CPU baseline on large instances
  \item<5-> \alert{Plot / table placeholders} --- to be lifted from paper \S 9.2--9.3
  \end{itemize}
\end{frame}

% =============================================================
\section{Conclusion}
% =============================================================
\begin{frame}{Summary}
  \begin{center}
    \url{https://github.com/exanauts/ExaModels.jl}
  \end{center}
  \vspace{1em}
  \begin{itemize}
  \item ExaModels offers a \alert{SIMD abstraction} for NLPs---an alternative to vectorization that preserves indexed structure through (pattern, data iterator) pairs
  \item<2-> Type-stable parameterized expression tree + coloring-free sparse AD make \alert{GPU-native} derivative evaluation practical
  \item<3-> One model, six KA.jl backends, no code change
  \item<4-> Already the modeling layer for MadNLP's GPU-native interior-point methods \citep{pacaudCondensedspaceMethodsNonlinear2024}
  \end{itemize}
  \medskip
  \visible<5->{
    \begin{block}{Discussion}
      \emph{Can ExaModels power JuMP on GPUs?}---concrete examples to walk through after the talk.
    \end{block}
  }
  \refbr{
    \fullcite{pacaudCondensedspaceMethodsNonlinear2024}
  }
\end{frame}

\AtNextBibliography{\tiny}
\setbeamertemplate{bibliography item}{}
\begin{frame}{References}
  \tikz[text width=1.1\textwidth,xscale=.9,transform shape]{
    \node at (current page.center) {\printbibliography[heading=none]};
  }
\end{frame}

\end{document}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: t
%%% End:
