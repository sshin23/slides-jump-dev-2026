# Figures pulled from Sungho's archive (Stash, 2026-05-29)

These are figures Sungho has actually used before (sourced from ~/refs/archive).
Prefer these over the generic Wikimedia downloads for authenticity.

## (1) Hardware — GPU illustrations he's used
- exa_a100_gpu.png   <- ~/refs/archive/figure/a100-14ee6909.png
    NVIDIA A100 GPU accelerator card with large heatsink + PCIe connector.
- exa_v100_gpu.jpg   <- ~/refs/archive/figure/v100.jpg
    NVIDIA V100 data-center GPU accelerator card.
- exa_l4_gpu.png     <- ~/refs/archive/figure/l4.png
    GPU accelerator card, passive heatsink, PCIe form factor (used in ge-vernova proposal).
- exa_gpu_generic.jpg <- ~/refs/archive/figure/gpu.jpg
    Professional GPU accelerator card with distinctive heatsink.
  (No standalone CPU-chip figure found in his archive — see Wikimedia CC0/CC-BY
   options in CREDITS-hardware.md for the CPU side of the motivation slide.)

## (2) ExaModels architecture
- exa_madsuite_architecture.pdf <- ~/refs/archive/figure/examodels.pdf
    MadSuite NLP framework: ExaModels + MadNLP + CUDA/linear-solver/AD/KKT components.
  (No standalone SIMDFunction / parameterized-expression-tree diagram found in the
   archive index — likely only in the paper TeX. Tell me if you want me to dig the
   paper source for a TikZ SIMD diagram.)

## (3) GPU 5-backend scaling
- exa_ad_scaling_benchmark.svg <- ~/refs/archive/figure/benchmark-2ee9bae5.svg
    ExaModels AD benchmark: speedup over JuMP/AMPL vs nvar (obj/cons/grad/jac/hess).
- exa_cpu_vs_gpu_benchmark.svg <- ~/refs/archive/figure/benchmark-a6dc7ce7.svg
    Execution-time comparison across datasets, CPU vs NVIDIA GPU.
  NOTE: the true 5-backend OPF scaling plot (CUDA / AMDGPU / oneAPI / Metal / CPU)
  is NOT committed as an image. The per-backend data is in
  ~/git/papers/exa-models-paper/benchmark/results/*.csv
  (shin-compute-001 CUDA Quadro GV100, shin-compute-002 AMDGPU Radeon VII,
   oneAPI Intel Arc A770, CPU-8T) and is regenerated by benchmark/plot_opf.jl
   / plot_opf_pgf.jl. Run that to get a fresh 5-backend plot.

## (b) Multi-backend OPF speedup (best-effort, auto-generated 2026-05-29)
- exa_backend_scaling.pdf / .png  (source: exa_backend_scaling_plot.jl)
    Per-callback (obj/cons/grad/jac/hess) ExaModels speedup vs the CPU 1-thread
    reference (shin-compute-001, Xeon 6130), log-log vs nvar.
    Backends present in the OPF data: CUDA (Quadro GV100), AMDGPU (Radeon VII),
    oneAPI fp64 (Intel Arc A770), CPU-8T. NO Metal run and NO oneAPI-fp32 OPF data
    exist in benchmark/results, so this is 4 backends, not 5.
    oneAPI fp64 is slow because Arc A770 fp64 is emulated (expected).
    Built directly from benchmark/results/*.csv (device strings read from the
    _hw.toml companions); does NOT use the committed collect.jl/plot_opf.jl,
    which are currently out of sync with the data (see note to Sungho).

## (a-bis) AD forward/reverse passes (fig:ad from sections/ad.tex)
- exa_ad_passes.pdf  (+ _tikz.tex source, _standalone.tex wrapper)
    Rosenbrock expression tree annotated with the forward pass (green: primal v +
    local derivatives) and reverse pass (red dashed: adjoint v-bar propagation),
    with gradient accumulations (df[a]+=400sa, etc.). Companion to exa_expression_tree.pdf.
