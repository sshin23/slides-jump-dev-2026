julia> @time em = build_v1(1000);
  1.307 s   # cold compile

julia> @time em = build_v1(1000);
  33 us     # cached pattern, no recompile

julia> @time em = build_v2(1000);   # *different* algebraic pattern
  169 ms    # fresh compile
